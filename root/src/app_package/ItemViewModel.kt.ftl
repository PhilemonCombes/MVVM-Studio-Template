package ${packageName}.${directoryName}

class ${activityClass}ItemViewModel(private val contract: ${activityClass}AdapterContract) : BaseItemViewModel<${itemObject}>() {
	
 	override fun onItemChanged(item: ${itemObject}) {
        
    }
}